name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: nextstrain/.github/.github/workflows/pathogen-repo-ci.yaml@master
    with:
      runtimes: |
        - docker
      env: |
        NEXTSTRAIN_DOCKER_IMAGE: "nextstrain/base:branch-victorlin-test-augur-1918"
